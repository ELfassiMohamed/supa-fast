# ---------------------------
# APPLICATION SETTINGS
# ---------------------------
spring.application.name=provider-service
server.port=8082
spring.main.allow-bean-definition-overriding=true

# Local secrets: Provider-Service/.env (gitignored). Run mvnw from the Provider-Service directory.
spring.config.import=optional:file:.env[.properties]
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration

# ---------------------------
# CONFIG SERVER CONFIGURATION
# ---------------------------
# Removed for Kubernetes deployment

# ---------------------------
# MONGODB CONFIGURATION
# ---------------------------
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/MaBase}
logging.level.org.mongodb.driver=WARN
logging.level.org.springframework.data.mongodb=INFO

# ---------------------------
# SPRING SECURITY
# ---------------------------
#spring.security.user.name=admin
#spring.security.user.password=password123
#spring.security.user.roles=USER

spring.main.allow-circular-references=true
#spring.main.allow-bean-definition-overriding=true

# ---------------------------
# JWT CONFIGURATION (set JWT_SECRET / JWT_EXPIRATION in .env or environment)
# ---------------------------
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ---------------------------
# RABBITMQ CONFIGURATION
# ---------------------------
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:guest}
spring.rabbitmq.connection-timeout=60000

spring.rabbitmq.listener.simple.retry.enabled=true
spring.rabbitmq.listener.simple.retry.max-attempts=3
spring.rabbitmq.listener.simple.retry.initial-interval=1000
spring.rabbitmq.listener.simple.acknowledge-mode=auto
spring.rabbitmq.listener.simple.concurrency=1
spring.rabbitmq.listener.simple.max-concurrency=10
spring.rabbitmq.listener.simple.prefetch=1

# ---------------------------
# JACKSON CONFIGURATION
# ---------------------------
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.time-zone=UTC

# ---------------------------
# LOGGING
# ---------------------------
logging.level.com.provider_service=DEBUG
logging.level.org.springframework.amqp=DEBUG
#logging.file.name=logs/provider-service.log

# ---------------------------
# SWAGGER / OPENAPI
# ---------------------------
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.operationsSorter=alpha
springdoc.packages-to-scan=com.provider_service.controllers


